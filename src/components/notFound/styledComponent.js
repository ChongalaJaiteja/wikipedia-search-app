import styled from "styled-components";

export const ErrorBgContainer = styled.div`
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
`;

export const ErrorImg = styled.img`
    width: min(80%, 30rem);
`;
